# API URL Configuration
# Leave empty for production (same-origin requests when backend serves frontend)
# Set to http://localhost:8001 for local development with separate backend
NEXT_PUBLIC_API_URL=

# Production: Backend serves frontend on same port, so no API URL needed
# Development: Set to http://localhost:8001 if running backend separately
