# AutoDoc Extractor - Frontend\n\nNext.js frontend for the AutoDoc Extractor application with Material-UI, TypeScript, and Recharts.\n\n## 🚀 Features\n\n- **File Upload**: Drag & drop interface for documents\n- **Real-time Processing**: Live status updates during document processing\n- **Editable Data Table**: MUI DataGrid for reviewing and editing extracted data\n- **Rich Insights**: Interactive charts and analytics with Recharts\n- **Image Preview**: Zoomable document preview with table highlighting\n- **Responsive Design**: Mobile-friendly Material-UI components\n- **TypeScript**: Full type safety and IntelliSense\n\n## 📁 Project Structure\n\n```\nfrontend/\n├── src/\n│   ├── components/          # Reusable React components\n│   │   ├── FileUpload.tsx   # Drag & drop file upload\n│   │   ├── EditableTable.tsx # MUI DataGrid for data editing\n│   │   ├── InsightsPanel.tsx # Charts and analytics\n│   │   ├── ImagePreview.tsx  # Document preview with zoom\n│   │   └── Loader.tsx       # Loading states\n│   ├── pages/               # Next.js pages\n│   │   ├── index.tsx        # Upload page\n│   │   ├── result/[job_id].tsx # Results page\n│   │   ├── _app.tsx         # App configuration\n│   │   └── _document.tsx    # Document configuration\n│   ├── services/            # API integration\n│   │   └── api.ts           # Backend API service\n│   ├── types/               # TypeScript definitions\n│   │   └── schema.ts        # Type definitions\n│   ├── utils/               # Utility functions\n│   │   └── createEmotionCache.ts # Emotion cache for SSR\n│   └── styles/              # Global styles\n│       └── globals.css      # Tailwind + custom CSS\n├── package.json             # Dependencies\n├── tsconfig.json           # TypeScript configuration\n├── tailwind.config.js      # Tailwind CSS configuration\n└── next.config.js          # Next.js configuration\n```\n\n## 🛠️ Installation & Setup\n\n### Prerequisites\n- Node.js 18+ and npm\n- Backend API running on http://localhost:8000\n\n### Install Dependencies\n\n```bash\ncd frontend\nnpm install\n```\n\n### Development Server\n\n```bash\nnpm run dev\n```\n\nFrontend will be available at: http://localhost:3000\n\n### Build for Production\n\n```bash\nnpm run build\nnpm start\n```\n\n## 🔧 Configuration\n\n### Environment Variables\n\nCreate `.env.local` file:\n\n```env\n# API Backend URL\nNEXT_PUBLIC_API_URL=http://localhost:8000\n\n# App Configuration\nNEXT_PUBLIC_APP_NAME=\"AutoDoc Extractor\"\nNEXT_PUBLIC_MAX_FILE_SIZE=10485760  # 10MB in bytes\n```\n\n### API Integration\n\nThe frontend automatically proxies API requests to the backend:\n- Frontend: `http://localhost:3000/api/*` \n- Backend: `http://localhost:8000/*`\n\nConfigured in `next.config.js`:\n\n```javascript\nasync rewrites() {\n  return [\n    {\n      source: '/api/:path*',\n      destination: 'http://localhost:8000/:path*'\n    }\n  ];\n}\n```\n\n## 📱 UI Components\n\n### FileUpload Component\n- Drag & drop interface\n- File type validation\n- Progress indication\n- Error handling\n\n```tsx\n<FileUpload\n  onUploadComplete={(response) => router.push(`/result/${response.job_id}`)}\n  onUploadError={(error) => setError(error)}\n  acceptedTypes={['image/jpeg', 'image/png', 'application/pdf']}\n  maxSize={10}\n/>\n```\n\n### EditableTable Component\n- MUI DataGrid integration\n- Inline editing\n- Add/remove rows\n- CSV export\n- Real-time calculations\n\n```tsx\n<EditableTable\n  data={extractedItems}\n  onDataChange={setExtractedItems}\n  onSave={handleSaveChanges}\n  isReadOnly={false}\n/>\n```\n\n### InsightsPanel Component\n- Category pie charts\n- Spending trend lines\n- Anomaly detection alerts\n- Interactive legends\n\n```tsx\n<InsightsPanel\n  insights={results.insights}\n  isLoading={false}\n/>\n```\n\n### ImagePreview Component\n- Zoom in/out controls\n- Pan functionality\n- Bounding box overlays\n- Fullscreen mode\n\n```tsx\n<ImagePreview\n  imageUrl={processedImageUrl}\n  boundingBoxes={detectedTables}\n  onImageLoad={() => setImageLoaded(true)}\n/>\n```\n\n## 🎨 Styling & Theming\n\n### Material-UI Theme\n\nCustom theme configured in `_app.tsx`:\n\n```typescript\nconst theme = createTheme({\n  palette: {\n    primary: { main: '#3b82f6' },\n    secondary: { main: '#10b981' },\n  },\n  typography: {\n    fontFamily: '\"Inter\", \"Roboto\", sans-serif',\n  },\n});\n```\n\n### Tailwind CSS\n\nUtility classes for rapid styling:\n\n```tsx\n<div className=\"w-full max-w-2xl mx-auto animate-fade-in\">\n  <Paper elevation={2} className=\"glass-morphism\">\n    {/* Content */}\n  </Paper>\n</div>\n```\n\n## 🔄 State Management\n\n### React Hooks Pattern\n\nUsing built-in React hooks for state management:\n\n```tsx\nconst [results, setResults] = useState<ResultResponse | null>(null);\nconst [loading, setLoading] = useState(true);\nconst [error, setError] = useState<string>('');\n\n// API integration with error handling\nuseEffect(() => {\n  const fetchResults = async () => {\n    try {\n      const data = await apiService.getResults(jobId);\n      setResults(data);\n    } catch (err) {\n      setError(err.message);\n    } finally {\n      setLoading(false);\n    }\n  };\n  \n  fetchResults();\n}, [jobId]);\n```\n\n## 📊 Data Flow\n\n### Upload Flow\n1. User selects file in `FileUpload` component\n2. File validation (type, size)\n3. Upload to backend `/upload` endpoint\n4. Navigate to results page with job ID\n5. Poll job status until completion\n\n### Processing Flow\n1. Backend processes document (OCR, table detection, ML)\n2. Frontend polls `/status/{job_id}` every 3 seconds\n3. Display progress updates to user\n4. Load results when status becomes 'completed'\n\n### Results Flow\n1. Display extracted data in editable table\n2. Show insights with interactive charts\n3. Allow user corrections and re-processing\n4. Provide CSV download and sharing options\n\n## 🧪 Testing\n\n### Component Testing\n\n```bash\nnpm run test\n```\n\n### Type Checking\n\n```bash\nnpm run type-check\n```\n\n### Linting\n\n```bash\nnpm run lint\n```\n\n## 📦 Deployment\n\n### Vercel (Recommended)\n\n```bash\nnpm install -g vercel\nvercel --prod\n```\n\n### Docker\n\n```bash\ndocker build -t autodoc-frontend .\ndocker run -p 3000:3000 autodoc-frontend\n```\n\n### Environment Configuration\n\nUpdate API URL for production:\n\n```env\nNEXT_PUBLIC_API_URL=https://your-backend-domain.com\n```\n\n## 🔧 Troubleshooting\n\n### Common Issues\n\n**CORS Errors:**\n- Ensure backend allows frontend domain in CORS settings\n- Check API URL configuration\n\n**Build Errors:**\n- Clear `.next` cache: `rm -rf .next`\n- Reinstall dependencies: `rm -rf node_modules && npm install`\n\n**TypeScript Errors:**\n- Check type definitions in `src/types/schema.ts`\n- Ensure all imports are correctly typed\n\n**Styling Issues:**\n- Verify Tailwind CSS is properly configured\n- Check Material-UI theme setup\n\n### Debug Mode\n\nEnable debug logging:\n\n```typescript\n// Add to api.ts\nconst DEBUG = process.env.NODE_ENV === 'development';\n\nif (DEBUG) {\n  console.log('API Request:', config);\n}\n```\n\n## 🤝 Contributing\n\n1. Follow TypeScript strict mode\n2. Use Material-UI components for consistency\n3. Add proper error boundaries\n4. Write tests for new components\n5. Follow accessibility guidelines\n\n## 📚 Resources\n\n- [Next.js Documentation](https://nextjs.org/docs)\n- [Material-UI Components](https://mui.com/components/)\n- [Recharts Examples](https://recharts.org/en-US/examples)\n- [TypeScript Handbook](https://www.typescriptlang.org/docs/)\n- [Tailwind CSS Utilities](https://tailwindcss.com/docs)"